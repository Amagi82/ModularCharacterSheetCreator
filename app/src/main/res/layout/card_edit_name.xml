<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="nameItemViewModel"
                  type="amagi82.modularcharactersheetcreator.ui.edit.name.NameItemViewModel"/>
    </data>

    <android.support.v7.widget.CardView
        style="@style/Card">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <android.support.design.widget.TextInputLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/default_keyline_padded_layout"
                android:layout_marginRight="@dimen/default_keyline_padded_layout"
                android:layout_marginTop="@dimen/default_margin"
                app:errorEnabled="false">

                <EditText
                    android:id="@+id/etName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:hint="@string/character_name"
                    android:imeOptions="actionDone"
                    android:inputType="textPersonName"
                    android:minWidth="@dimen/toolbar_edittext_minwidth"
                    android:singleLine="true"
                    android:text="@{nameItemViewModel.name}"
                    android:textAppearance="@style/TextAppearance.AppCompat.Headline"/>

            </android.support.design.widget.TextInputLayout>

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/default_margin"
                android:layout_marginRight="@dimen/default_margin"
                android:layout_marginTop="@dimen/default_margin"
                app:imageUri="@{nameItemViewModel.imageUri}"/>

            <ImageView
                android:id="@+id/actionPhoto"
                style="@style/ActionButton"
                android:src="@drawable/ic_camera_alt_24dp"
                android:tint="@color/primary_dark"/>

        </LinearLayout>
    </android.support.v7.widget.CardView>
</layout>